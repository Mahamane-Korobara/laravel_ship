<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Chemin de base des projets déployés
    |--------------------------------------------------------------------------
    */
    'base_path' => env('DEPLOY_BASE_PATH', '/var/www/projects'),

    /*
    |--------------------------------------------------------------------------
    | Nombre maximum de releases à conserver par projet
    |--------------------------------------------------------------------------
    */
    'max_releases' => env('DEPLOY_MAX_RELEASES', 5),

    /*
    |--------------------------------------------------------------------------
    | Email admin pour les certificats SSL Let's Encrypt
    |--------------------------------------------------------------------------
    */
    'admin_email' => env('ADMIN_EMAIL', 'admin@example.com'),

    /*
    |--------------------------------------------------------------------------
    | Sauvegardes MySQL locales (jours de rétention)
    |--------------------------------------------------------------------------
    */
    'backup_retention_days' => env('DEPLOY_BACKUP_RETENTION_DAYS', 30),

    /*
    |--------------------------------------------------------------------------
    | Horaire des sauvegardes (format HH:MM)
    |--------------------------------------------------------------------------
    */
    'backup_schedule' => env('DEPLOY_BACKUP_SCHEDULE', '02:00'),

    /*
    |--------------------------------------------------------------------------
    | Logrotate (nombre de rotations)
    |--------------------------------------------------------------------------
    */
    'logrotate_rotate' => env('DEPLOY_LOGROTATE_ROTATE', 14),
];
